"""
Tests: Appointment booking, conflict detection, cancel, reschedule
Run: pytest tests/test_appointments.py -v
"""
import pytest
# TODO: Add fixtures for creating doctor + availability + patient
# then test: book, double-book rejection, cancel, reschedule
