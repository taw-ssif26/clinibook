"""
Tests: Recurring series creation, cancel one, cancel all
Run: pytest tests/test_series.py -v
"""
import pytest
# TODO: Add fixtures + integration tests for series creation,
#       single instance cancel, full series cancel
